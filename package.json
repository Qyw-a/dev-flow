{
  "name": "@branch-manager/monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Branch Manager monorepo",
  "scripts": {
    "dev": "pnpm --filter @branch-manager/desktop dev",
    "build": "pnpm --filter @branch-manager/desktop build",
    "typecheck": "pnpm --filter @branch-manager/desktop typecheck",
    "preview": "pnpm --filter @branch-manager/desktop preview",
    "postinstall": "pnpm --filter @branch-manager/desktop postinstall"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "electron-builder",
      "esbuild"
    ]
  }
}
